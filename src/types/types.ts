export interface IArticles {
  id: string;
  title: string;
  url: string;
  imageUrl: string;
  publishedAt: string;
  newSite: string;
  summary: string;
}
export interface INews {
  id: string;
  title: string;
  url: string;
  imageUrl: string;
  publishedAt: string;
  newSite: string;
  summary: string;
}
export interface ITab {
  id: string;
  label: string;
}
