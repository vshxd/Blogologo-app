export interface IArticles {
  id: number;
  title: string;
  url: string;
  imageUrl: string;
  publishedAt: string;
  newSite: string;
}
export interface ITab {
  id: string;
  label: string;
}

export interface INews {
  id: number;
  title: string;
  url: string;
  imageUrl: string;
  publishedAt: string;
  newSite: string;
}
